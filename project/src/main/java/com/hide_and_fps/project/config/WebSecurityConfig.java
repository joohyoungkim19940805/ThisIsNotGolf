package com.hide_and_fps.project.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.PropertySource;
import org.springframework.context.annotation.PropertySources;

/*
@Configuration
@PropertySources({
	//로컬 환경에서 사용될 파일 경로
	@PropertySource(value = "classpath:/appConfig/security.properties", ignoreResourceNotFound = true),
	
	//서버 환경에서 사용될 파일 경로 (서버에서 사용될 security.properties 파일은 GIT에 올리지 않도록 주의 필요)
	@PropertySource(value = "file:${appConfigPath}/security.properties", ignoreResourceNotFound = true)
})
public class WebSecurityConfig {
	
}
*/